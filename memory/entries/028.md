---
type: correction
title: "JSX component children arrive as an array (`children: [child]`); html\`\` as a single child — ssr's walkChild/walkChildGen must iterate arrays, not just single children (fixed)"
description: "Verified 2026-07-28 (fixed same day). babel-plugin-hellajs compiles <Comp>{child}</Comp> to component(Comp, { children: [child] }) — an ARRAY; html\`\` compiles <${Comp}>${child}</${Comp}> to props.children = child (single). The ssr walkers classified only single children and DROPPED arrays (fell through to 'nothing'), so JSX <Suspense>{...}</Suspense> streamed an EMPTY staged <template>. Fix: walkChild (sync, lib/ssr.ts) and walkChildGen (async, lib/internal/walk.ts) now branch on Array.isArray → delegate to walkChildren/walkChildrenGen; html\`\` single-child output is byte-identical (parity preserved)."
tags: [arch, ssr, dom, jsx, babel, contract]
timestamp: 2026-07-28
last_confirmed: 2026-07-28
triggers: [ssr-component-children, jsx-array-children, suspense-streaming, ssr-walker, html-vs-jsx]
---

> **Naming superseded 2026-08-22 (entry 045):** `ssrAsync`→`ssr.async`, `ssrStream`→`ssr.stream`, `docStream`→`doc` (stream overload) — the old names below are the pre-v2 API, kept for history.

# Why

A streaming-SSR example (JSX + vite-plugin-hellajs on the server) shipped an empty staged
`<template>`: the shell and `<Suspense>` fallback flushed, but the resolved children never staged. Root
cause: the `renderDynamicGen`/`renderDynamic` Suspense case passed `props.children` straight to the
single-child walker. `walkChild`/`walkChildGen` classify `string | number | function | object-with-raw
| object-with-tag` and fall through everything else (incl. arrays) to "nothing". The catch: the two
authors of a component's children produce DIFFERENT shapes —

- `html\`\`` parser: `<${Suspense}>${getter}</${Suspense}>` → `props.children = getter` (single).
- `babel-plugin-hellajs`: `<Suspense>{getter}</Suspense>` → `component(Suspense, { children: [getter] })` (ARRAY).

All ssr Suspense tests use the `html\`\`` single-child shape, so the JSX array case was untested and
silently produced empty staged templates. The fix is in the walkers (the root), not the Suspense case,
so it also covers Transition/Lazy/any isDynamic prop that can be an array — and `[fn]` (one element)
iterates to the same `MARK_OPEN…MARK_CLOSE` wrapping as a single `fn`, so `html\`\`` output is
unchanged (the parity tests stay green).

Recall this before editing the ssr walkers or adding an isDynamic `SsrMeta.kind`: component children
can be single OR array depending on the authoring syntax, and the walkers must handle both. The docs
claim "JSX and html compile to the same AST" — true for the tree, but component-children SHAPE differs.

# Evidence

- `packages/ssr/lib/ssr.ts` `walkChild` and `packages/ssr/lib/internal/walk.ts` `walkChildGen` — both
  now have `if (Array.isArray(child)) return walkChildren(child);` / `yield* walkChildrenGen(child,
  pending); return;` immediately after the falsy guard.
- `packages/ssr/tests/ssr-suspense.test.ts` — two new cases use `component(Suspense, { children: [...] })`
  (the exact babel-emitted shape): "ssr (sync) renders array children directly" and "ssrStream stages
  resolved array children" — both assert the same output as the single-child `html\`\`` cases (parity).
- `bun coverage ssr` → 134 pass (was 132), 100% lines; `bun coverage dom` → 317 pass (blast radius:
  hydrate consumes ssr output); lint green.
- Empirical: `html\`\`` Suspense → `props.children` is a function (`isArray: false`); JSX
  `component(Suspense, …)` → `props.children` is `[fn]` (`isArray: true`) — confirmed by dumping the node.
- Related: memory 013 (ssr markers), 024 (HellaNode field names) — neither covers children shape.
