---
type: decision
title: Run empirical package checks as `bun -e` from packages/<pkg> — @hellajs/* subpath imports do not resolve from /tmp scratch files
description: Quick behavioral probes of a workspace package (constructor names, render output, public-API sanity) run as `cd packages/<pkg> && bun -e '…'` with dynamic imports — a scratch .ts outside the repo tree fails to resolve `@hellajs/<pkg>/bundle` because workspace linking only works via node_modules walk-up from inside the repo.
tags: [toolchain, workspaces, testing]
timestamp: 2026-08-27
last_confirmed: 2026-08-27
triggers: [empirical-probe, scratch-script, subpath-import, bun-e]
---

# Why

Root `node_modules/@hellajs/*` symlinks (→ `packages/*`) resolve only through Node/Bun walk-up from a path inside the repo. A scratch script under `/tmp` has no such chain, so `import "@hellajs/dom/bundle"` fails there — and writing scratch files outside the repo also violates the repo's own rule ("use existing tests, examples, and folders in the repo; do not wander outside the file system to test or build"). The sanctioned, working alternative needs no file at all: run `bun -e` (or `bun --eval`) with the working directory inside the target package, using dynamic `await import(...)` for runtime modules (`@happy-dom/global-registrator`, `@hellajs/<pkg>/bundle`) since `-e` input is treated as a module and top-level await is available.

# Evidence

Verified 2026-08-27 during the dom behavior-gaps investigation:
- Failing form: `bun /tmp/svg-public.ts` → `error: Cannot find module '@hellajs/dom/bundle' from '/tmp/svg-public.ts'` (Bun v1.3.3).
- Working form: `cd packages/dom && bun -e 'import { GlobalRegistrator } from "@happy-dom/global-registrator"; GlobalRegistrator.register(); const { html, mount } = await import("@hellajs/dom/bundle"); …'` → resolves, runs, and produced the probe result (`svg ctor: HTMLUnknownElement`) that grounded plan unit `plans/dom/code/behavior-gaps/01-svg-namespace.md`.
- Related but distinct: memory 026 covers `examples/` resolving `@hellajs/*` via root symlinks and warns against `bun add` there; this entry covers ad-hoc probe scripts, where the failure mode is module resolution from outside the repo tree.
