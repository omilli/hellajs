---
type: decision
title: "hydrateNode tag comparison is exact-case for foreign-namespace elements — SVG/MathML parsers keep authored tag case, so uppercase-folding false-mismatches every camelCase tag"
description: DOM parsers (browsers and HappyDOM alike) preserve authored case for elements created in a foreign namespace — innerHTML `<svg><clipPath>` yields tagName "clipPath" (SVGClipPathElement, SVG ns) — so any hydrate/adopt tag comparison must be exact-case when the existing element's namespaceURI is non-HTML (`tagMatches` in packages/dom/lib/internal/hydrate.ts); folding to `tag.toUpperCase()` compares "clipPath" against "CLIPPATH", warns a false mismatch, and replaceMismatch subtree-replaces a perfectly matching server node. Companion facts from the same fix — element creation must go through `createElementNS` under foreign-ns parents or tags mount as HTMLUnknownElement and render nothing (`childNamespaceOf`/`HTML_NS` in packages/dom/lib/internal/render.ts); HappyDOM has no MathMLElement class, so MathML tests assert `namespaceURI`, not constructors; and lib.dom types `localName`/`namespaceURI` on `Element` only — a helper typed `(parent: Node | null)` must cast to `Element` before reading them or tsc fails TS2339.
tags: [dom, hydrate, svg]
timestamp: 2026-08-30
last_confirmed: 2026-08-30
triggers: [hydrate-tag-compare, svg-namespace, tagmatches, foreign-namespace]
---

# Why

The HTML parser uppercases `tagName` for HTML elements but switches to foreign-content mode inside `<svg>`/`<math>`, preserving authored case (`clipPath`, `linearGradient`, `feGaussianBlur`). Two dom behaviors hinge on this and both break if treated as HTML:

1. **Tag comparison during hydrate.** `hydrateNode`'s original `element.tagName !== tag.toUpperCase()` assumed HTML folding. For an adopted SVG element it compared `"clipPath" !== "CLIPPATH"` → false mismatch warning + `replaceMismatch` subtree replacement of a node the server rendered correctly. The fix is the local `tagMatches(element, tag)` helper: uppercase-fold only when `element.namespaceURI` is the HTML ns, exact-case otherwise. Any future code that walks/adopts existing DOM and compares tags (selective hydration touches the same file — plans/dom/code/behavior-gaps/04) must reuse this predicate, not re-derive a fold.
2. **Element creation.** `document.createElement("clipPath")` produces `HTMLUnknownElement` that renders nothing; under a foreign-ns parent creation must use `createElementNS(parentNs, tag)`. The derivation lives in `childNamespaceOf(parent)` — one source of truth; the ns param on `mountNode`/`resolveNode`/`appendToParent` is consumed only at creation, children re-derive from the live parent. `foreignObject` is an HTML integration point: `childNamespaceOf` returns undefined for it so its children reset to HTML.

The namespace-threading architecture is documented in packages/dom/AGENTS.md (`mountNode`/`appendToParent` section) — read that for the call-site map; this concept carries the parser-level facts and test-authoring quirks that AGENTS.md does not.

# Evidence

- HappyDOM probe (bun -e, GlobalRegistrator): `createElementNS(SVG_NS, t)` → SVGSVGElement / SVGClipPathElement / SVGLinearGradientElement / SVGCircleElement / SVGRectElement / SVGTextElement / SVGForeignObjectElement; `createElementNS(MATHML_NS, …)` → generic `Element` with the MathML ns (no MathMLElement class — assert `namespaceURI`, never a MathML constructor). `innerHTML = "<svg><clipPath …>"` → tagName `"clipPath"`, SVG ns — case preserved through the parser, not just createElementNS.
- Pre-fix: `bun test packages/dom/tests/svg.test.ts` → 8/8 fail (`HTMLUnknownElement` constructors; hydrate test captured 1 `[dom] hydrate mismatch` warning via `suppressWarn`). Post-fix: 9/9 pass, 0 warnings, `bun coverage dom` exit 0 (373 pass / 0 fail).
- tsc failure that surfaced the Element-only typing: `packages/dom/lib/internal/render.ts(25,25): error TS2339: Property 'localName' does not exist on type 'Node'` (same for `namespaceURI`) — fixed by casting to `Element` inside `childNamespaceOf`.
- Source: `tagMatches` + both call sites in `packages/dom/lib/internal/hydrate.ts`; `childNamespaceOf`/`HTML_NS`/ns-aware `mountNode` creation in `packages/dom/lib/internal/render.ts`; regression tests in `packages/dom/tests/svg.test.ts`.
