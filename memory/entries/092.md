---
type: correction
title: hydrate() tests must pass the SAME node instance ssr() rendered — a second template call leaks a server-realm component effect into mock counts
description: In hydrate tests, hoist ONE html`` call and pass it to both ssrContainer() and hydrate(); calling the template twice runs component() twice, and the ssr-pass instance's effect (scoped to a discarded wrapper node) never disposes — it re-fires on every signal change and reads as a phantom leak.
tags: testing, dom, ssr, hydration
timestamp: 2026-09-06
last_confirmed: 2026-09-06
triggers: hydrate-test-authoring, ssr-hydrate-same-node, phantom-effect-leak
---
# Why

`html\`\`` evaluates its component interpolations at CALL time (`cloneWithValues` → `component()`), so `hydrate(html\`<div><${C} /></div>\`, container)` next to `ssrContainer(html\`<div><${C} /></div>\`)` builds TWO component instances from one literal. The ssr-pass instance's dispose lives on a wrapper node nobody mounts (server output is stringified), so its body effect is never disposed — in the shared test realm it keeps firing on every signal write. A test asserting "N calls after unmount" then sees N+1 and misdiagnoses a scope-wiring leak that is actually test setup. The documented contract (`packages/dom/lib/hydrate.ts` JSDoc): "Pass the SAME node the server passed to ssr()". Hoist `const view = html\`...\`` once; `ssrContainer(view)` then `hydrate(view, container)` — one instance, whose scope the hydrate walker wires onto the adopted DOM.

# Evidence

Verified 2026-09-06 during the component-static-scope fix run: the hydrate scenario test failed with "Expected 2, Received 3" when the template was called twice (assert after hydrate passed at 2; one extra fire after `count(1)` — exactly one leaked instance); switching to a single hoisted `view` instance drove the same assertions green (`bun coverage dom`, packages/dom/tests/component.test.ts "wires the scope when hydrate adopts a static root"). Note this does NOT affect production SSR (server and client are separate realms; the server instance is garbage with the server process) — it is a same-realm test-authoring hazard.
