---
title: registry
description: Low-level lifecycle management API for DOM elements. Manages effects, events, and hooks attached to elements.
layout: ../../../layouts/MainLayout.astro
---

import RegistryContent from '@dom/api/registry.mdx'

import { Icon } from "astro-icon/components";

# registry

Low-level lifecycle management API for DOM elements. Manages effects, events, and hooks attached to elements.

<div role="alert" class="alert alert-info alert-soft text-base">
  <Icon name="ph:info" width={24} height={24} />
  <span>This is mostly an internal API. You typically don't need to use this directly unless building custom rendering utilities or framework integrations.</span>
</div>

<RegistryContent />
