---
title: signalMap
description: Holds a Map with one signal per key, so value writes wake only that key's readers while membership changes wake collection-wide readers.
layout: ../../../layouts/MainLayout.astro
---

import SignalmapContent from '@core/api/signalmap.mdx'

<SignalmapContent />
