---
title: signalArray
description: Holds an array with one signal per element, so value writes wake only the readers of that element and in-place structural operations replace immutable spreads.
layout: ../../../layouts/MainLayout.astro
---

import SignalarrayContent from '@core/api/signalarray.mdx'

<SignalarrayContent />
