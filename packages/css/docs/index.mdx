## CSS

Type-safe CSS-in-JS with runtime style generation, reactive CSS variables, and automatic memory management. Scoped styles via the `name` option with reference-counted cleanup; global by default.

### Installation

```bash
npm install @hellajs/core @hellajs/css
```

### Example

```jsx
import { signal } from '@hellajs/core';
import { css } from '@hellajs/css';

// Create scoped styles
const cardStyle = css({
  padding: '1rem',
  borderRadius: '8px',
  backgroundColor: '#f8f9fa',

  // Nested selectors and pseudo-states
  'h2': { marginTop: 0 },
  '&:hover': {
    backgroundColor: '#e9ecef',
    transform: 'translateY(-2px)'
  },

  // Responsive design
  '@media (max-width: 768px)': {
    padding: '0.5rem'
  }
}, { name: 'card' });

const Card = ({ title, children }) => (
  <div class={cardStyle}>
    <h2>{title}</h2>
    {children}
  </div>
);
```

### API

- **[css](/reference/css/css)**: Create scoped styles with nested selectors and pseudo-states
- **[cssVars](/reference/css/cssvars)**: Reactive CSS custom properties that update when signals change
- **[cssRemove](/reference/css/cssremove)**: Manually remove a generated stylesheet
- **[cssReset](/reference/css/cssreset)**: Remove all generated stylesheets
- **[cssVarsRemove](/reference/css/cssvarsremove)**: Remove specific CSS variable declarations with reference counting
- **[cssVarsReset](/reference/css/cssvarsreset)**: Remove all generated CSS variable stylesheets

### Concepts

- **[Styling](/learn/concepts/styling)**: Traditional CSS classes and CSS-in-JS approaches

### Patterns

- **[Styling](/learn/patterns/styling)**: Scoped styles, themes, reactive CSS variables
